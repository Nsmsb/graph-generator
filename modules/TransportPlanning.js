module.exports = class TransportPlanning {
    constructor(path, {walk, bus, metro, tramway}) {

        // getters and setters
        this.getPath = () => path;

        this.getPlanning = () => {
            walk,
            bus,
            metro,
            tramway
        };

        this.getPlan
    }
}

